<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oose.postop">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- permission to receive messages from GCM -->
    <uses-permission android:name="android.c2dm.permission.SEND" />
    <uses-permission android:name="android.c2dm.permission.RECEIVE" />
    <!-- adding internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomepageActivity">
            <intent-filter>

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <receiver
            android:name="com.oose.postop.GcmBroadcastReceiver"
            android:exported="true"
            android:permission="com.google.android.c2dm.permission.SEND" >
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <category android:name="com.oose.postop" />
            </intent-filter>
        </receiver>
        <receiver android:process=":remote"
            android:name="com.oose.postop.NotificationCountAlarm">
        </receiver>
        <receiver android:process=":remote"
            android:name="com.oose.postop.PushNotificationAlarm">
        </receiver>
        <service
            android:name="com.oose.postop.RegistrationIntentService"
            android:exported="false">
        </service>
        <service
            android:name="com.oose.postop.GoogleFitFetchService"
            android:label="Google Fit Fetch">
        </service>

    </application>

</manifest>